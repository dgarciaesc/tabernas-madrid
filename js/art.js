/* ============================================================
   Grabados ilustrativos de reserva (SVG inline, sin red).
   Este juego usa fotos reales en todas las etapas y pantallas
   (ver `photo` en cada bloque de datos.js/worker.js), así que este
   objeto se deja vacío a propósito: si alguna pantalla se queda algún
   día sin foto, artCard() no pinta nada en vez de mostrar un grabado
   que no le corresponde (ver js/app.js, función artCard()).
   ============================================================ */

const STAGE_ART = {};
